import tretiakov from 'video/tretiakov.ogg';
import malutin from 'video/malutin.ogg';

const video = {
  tretiakov,
  malutin
}

export default function videoReducer(state = video) {
      return state;
}
